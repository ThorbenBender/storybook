import { Meta, Story, Canvas, ArgsTable, Description } from '@storybook/blocks';
import { AppComponent } from './app.component';

<Meta 
  title="Components/AppComponent" 
  component={AppComponent}
  parameters={{
    docs: {
      description: {
        component: 'The main application component that serves as the root of the Angular application.'
      }
    }
  }}
/>

# AppComponent

The `AppComponent` is the root component of the Angular application. It initializes the application and provides the main structure.

## Overview

This component:
- Sets the application title
- Implements OnInit lifecycle hook for initialization
- Provides a test method for debugging purposes
- Uses RouterOutlet for navigation

## Properties

| Property | Type | Default | Description |
|----------|------|---------|-------------|
| title | string | 'angular_test' | The application title |

## Methods

### ngOnInit()
Called after the component is initialized. Currently logs 'test' to the console.

### test()
A utility method that logs 'test' to the console for debugging purposes.

## Usage

```typescript
import { AppComponent } from './app.component';

// The component is automatically bootstrapped as the root component
```

## Stories

<Canvas>
  <Story name="Default">
    {{
      component: AppComponent,
      props: {
        title: 'angular_test'
      }
    }}
  </Story>
</Canvas>

<Canvas>
  <Story name="Custom Title">
    {{
      component: AppComponent,
      props: {
        title: 'My Custom App'
      }
    }}
  </Story>
</Canvas>

## Component API

<ArgsTable of={AppComponent} />

## Implementation Details

The component uses:
- **Standalone**: True (no NgModule required)
- **Selector**: `app-root`
- **Template**: External HTML file
- **Styles**: SASS stylesheet
- **Imports**: RouterOutlet for routing functionality

## GitHub Wiki Export

This documentation can be exported to GitHub wiki format by copying the markdown content above the stories section.